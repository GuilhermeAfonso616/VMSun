services:
  vms-postgres:
    image: postgres:16-alpine
    container_name: vms-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: "${POSTGRES_DB:-vmsun}"
      POSTGRES_USER: "${POSTGRES_USER:-vmsun}"
      POSTGRES_PASSWORD: "${POSTGRES_PASSWORD:-vmsun}"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER:-vmsun} -d $${POSTGRES_DB:-vmsun}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    ports:
      - "${POSTGRES_BIND_ADDRESS:-127.0.0.1}:${POSTGRES_PORT:-5433}:5432"
    volumes:
      - vms_postgres_data:/var/lib/postgresql/data

  vms-web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vms-web
    restart: unless-stopped
    command: ["python3", "main.py"]
    ports:
      - "8000:8000"
    env_file:
      - .env.docker
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      APP_HOST: 0.0.0.0
      APP_PORT: 8000
      APP_ROLE: "web"
      PRODUCT_MODE: "vms"
      ONVIF_WSDL_DIR: /app/wsdl
      HIK_SDK_ENABLED: "${HIK_SDK_ENABLED:-true}"
      HIK_SDK_LIB_DIR: /opt/hikvision/lib
      DAHUA_SDK_ENABLED: "${DAHUA_SDK_ENABLED:-true}"
      DAHUA_SDK_LIB_DIR: /opt/dahua/lib
      CAMERA_GATEWAY_ENABLED: "true"
      CAMERA_GATEWAY_BASE_URL: "http://vms-camera-gateway:8090"
      WEBRTC_GATEWAY_ENABLED: "true"
      WEBRTC_GATEWAY_API_BASE_URL: "http://vms-mediamtx:9997"
      WEBRTC_GATEWAY_RTSP_BASE_URL: "rtsp://vms-mediamtx:8554"
      MEDIA_BACKBONE_RECONCILE_ENABLED: "true"
    depends_on:
      vms-postgres:
        condition: service_healthy
      vms-control:
        condition: service_healthy
      vms-camera-gateway:
        condition: service_healthy
      vms-mediamtx:
        condition: service_started
    volumes:
      - ./data:/data
      - ./wsdl:/app/wsdl:ro
      - /var/run/docker.sock:/var/run/docker.sock
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "5"
        labels: "service=vms-web"

  vms-control:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vms-control
    restart: unless-stopped
    command: ["python3", "main.py"]
    ports:
      - "127.0.0.1:${RUNTIME_INTERNAL_PORT:-8001}:8001"
    healthcheck:
      test: ["CMD-SHELL", "python3 -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:8001/internal/health/ready', timeout=3)\" >/dev/null 2>&1 || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 30s
    env_file:
      - .env.docker
    extra_hosts:
      - "host.docker.internal:host-gateway"
    environment:
      APP_HOST: 0.0.0.0
      APP_PORT: 8001
      APP_ROLE: "control"
      PRODUCT_MODE: "vms"
      ONVIF_WSDL_DIR: /app/wsdl
      CAMERA_GATEWAY_ENABLED: "true"
      CAMERA_GATEWAY_BASE_URL: "http://vms-camera-gateway:8090"
      WEBRTC_GATEWAY_ENABLED: "true"
      WEBRTC_GATEWAY_API_BASE_URL: "http://vms-mediamtx:9997"
      WEBRTC_GATEWAY_RTSP_BASE_URL: "rtsp://vms-mediamtx:8554"
      MEDIA_BACKBONE_RECONCILE_ENABLED: "true"
    depends_on:
      vms-postgres:
        condition: service_healthy
      vms-camera-gateway:
        condition: service_healthy
      vms-mediamtx:
        condition: service_started
    volumes:
      - ./data:/data
      - ./wsdl:/app/wsdl:ro
      - /var/run/docker.sock:/var/run/docker.sock
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "5"
        labels: "service=vms-control"

  vms-camera-gateway:
    build:
      context: ./gateway
      dockerfile: Dockerfile
    container_name: vms-camera-gateway
    restart: unless-stopped
    ports:
      - "8090:8090"
    entrypoint: ["/bin/sh", "-c", "/app/gateway >> /logs/gateway.log 2>&1"]
    env_file:
      - .env.docker
    environment:
      GATEWAY_ADDR: ":8090"
      GATEWAY_FFMPEG_PATH: "ffmpeg"
      GATEWAY_STREAM_POLL_MS: "200"
      GATEWAY_FRAME_STALE_AFTER_SECONDS: "10"
      GATEWAY_OUTPUT_FPS: "2"
      GATEWAY_JPEG_QUALITY: "12"
      GATEWAY_RTSP_TRANSPORT: "tcp"
      GATEWAY_RECONNECT_DELAY_SECONDS: "5"
      GATEWAY_HEALTH_CHECK_INTERVAL_SECONDS: "2"
      GATEWAY_NODE_MAX_ACTIVE_CAMERAS: "16"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8090/healthz >/dev/null 2>&1 || exit 1"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 5s
    volumes:
      - ./data/logs:/logs
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "5"
        labels: "service=vms-camera-gateway"

  vms-mediamtx:
    image: bluenviron/mediamtx:1
    container_name: vms-mediamtx
    restart: unless-stopped
    command: ["/mediamtx.yml"]
    env_file:
      - .env.docker
    ports:
      - "${WEBRTC_GATEWAY_RTSP_BIND_ADDRESS:-0.0.0.0}:8554:8554/tcp"
      - "${WEBRTC_GATEWAY_BIND_ADDRESS:-0.0.0.0}:8889:8889"
      - "${WEBRTC_GATEWAY_BIND_ADDRESS:-0.0.0.0}:8189:8189/udp"
      - "${WEBRTC_GATEWAY_BIND_ADDRESS:-0.0.0.0}:8189:8189/tcp"
    volumes:
      - ./webrtc-gateway/mediamtx.yml:/mediamtx.yml:ro
      - ./data/logs:/logs
    environment:
      MTX_CONF: /mediamtx.yml
      MTX_WEBRTCADDITIONALHOSTS: "${MTX_WEBRTCADDITIONALHOSTS:-}"
    logging:
      driver: "json-file"
      options:
        max-size: "100m"
        max-file: "5"
        labels: "service=vms-mediamtx"

volumes:
  vms_postgres_data:
